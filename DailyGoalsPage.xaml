<Page x:Class="HabitTracker.DailyGoalsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="{DynamicResource WindowBackgroundBrush}"
      Title="Daily Goals">

    <Grid Margin="35,30,35,30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,6">
            <TextBlock Text="Daily Goals" Style="{DynamicResource PageTitleStyle}" VerticalAlignment="Center"/>

            <Button Content="+ Add Goal"
                    Width="140"
                    Height="40"
                    Margin="25,0,0,0"
                    Style="{DynamicResource PrimaryButtonStyle}"
                    Click="BtnAddGoal_Click"/>
        </StackPanel>

        <!-- Today -->
        <TextBlock x:Name="TxtToday"
                   Grid.Row="1"
                   Style="{DynamicResource PageSubtitleStyle}"
                   Margin="0,0,0,20"/>

        <!-- Goals List -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="GoalsPanel"/>
        </ScrollViewer>
    </Grid>
</Page>
